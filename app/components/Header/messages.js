import { defineMessages } from 'react-intl';

export const scope = 'app.components.Header';

export default defineMessages({
  github: {
    id: `${scope}.github`,
    defaultMessage: 'Github Users',
  },
  counter: {
    id: `${scope}.counter`,
    defaultMessage: 'Counter',
  },
});
