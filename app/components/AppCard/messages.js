/*
 * AppCard Messages
 *
 * This contains all the text for the AppCard component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AppCard';

export default defineMessages({
  learnMore: {
    id: `${scope}.learnMore`,
    defaultMessage: 'Learn More',
  },
});
